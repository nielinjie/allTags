// Generated by CoffeeScript 1.3.3
var pairs, syncGet;

pairs = function(items) {
  var a, b, result, _i, _j, _len, _len1;
  result = [];
  for (_i = 0, _len = items.length; _i < _len; _i++) {
    a = items[_i];
    for (_j = 0, _len1 = items.length; _j < _len1; _j++) {
      b = items[_j];
      if (b !== a) {
        result.push([a, b]);
      }
    }
  }
  console.log(result);
  return _.uniq(result, false, function(a) {
    return JSON.stringify(a.sort());
  });
};

syncGet = function(url) {
  var got;
  got = null;
  $.ajax({
    url: url,
    async: false,
    cache: false,
    success: function(data) {
      return got = data;
    }
  });
  return JSON.parse(got);
};
